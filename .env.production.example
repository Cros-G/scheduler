# Production env. Copy to .env.production and adjust as needed.
# .env.production is gitignored.

DATABASE_URL=file:/data/scheduler.db
DATA_DIR=/data
NODE_ENV=production
TZ=Asia/Shanghai
PORT=3000
HOSTNAME=0.0.0.0
HOST_PORT=3000
